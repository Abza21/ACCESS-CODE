// src/components/NFTs.js

import React from 'react';
import { Box, Typography } from '@mui/material';

const NFTs = () => {
  return (
    <Box sx={{ p: 3 }}>
      <Typography variant="h4">NFTs</Typography>
      <Typography variant="body1">Explore and manage your NFTs here.</Typography>
    </Box>
  );
};

export default NFTs;
